<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Background color, you can change it as needed -->
    <stroke
        android:width="1dp"
        android:color="@android:color/transparent" /> <!-- No stroke -->
    <corners android:radius="8dp" /> <!-- Radius for rounded corners, adjust as needed -->
</shape>